from server.websocket import Server

if __name__ == "__main__":
    print("Iniciando o servidor WebSocket...")
    # Inicia o servidor em uma thread separada, se necessário
    # Aqui, apenas instanciamos o servidor para simplificação
    server = Server()
    
    
